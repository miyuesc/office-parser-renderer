import { ST_Border } from '@ai-space/definitions/autogen/wml';

/**
 * Supported border styles.
 * These map to common border styles found in DOCX, XLSX, and PPTX.
 */
export type BorderStyle = ST_Border;
