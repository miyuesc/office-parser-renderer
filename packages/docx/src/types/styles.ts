/**
 * DOCX 样式相关类型定义
 *
 * 此文件包含样式、字体、边框等样式相关的类型定义
 * 由于文件较大，完整类型将在后续完善
 */

// 基础样式接口（完整定义待补充）
export interface DocxStyles {
  // 待补充完整定义
  [key: string]: any;
}

export interface NumberingDefinition {
  // 待补充完整定义
  [key: string]: any;
}

// 注：此文件需要从原 types.ts 补充完整的样式相关类型定义
// 包括：DocxStyle, CharacterStyle, ParagraphStyle, Border, Shading 等
